name = "quiet-nature-stats"
main = "worker.js"
compatibility_date = "2024-09-23"

# KV 存储绑定 ANALYTICS 不写死在这里（避免空 ID 导致部署报错）。
# 请到 Cloudflare 控制台 → 该 Worker → Settings → Variables → KV 绑定，
# 添加变量名 ANALYTICS，选择你创建的 KV 命名空间，保存即可。
